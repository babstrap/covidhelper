import 'package:flutter/material.dart';

class StructsFragment extends StatelessWidget {
  const StructsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Structs fragment"),
    );
  }
}
