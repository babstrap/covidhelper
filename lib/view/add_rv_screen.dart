import 'package:flutter/material.dart';

class AddRvScreen extends StatelessWidget {
  const AddRvScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add RV"),
      ),
    );
  }
}
